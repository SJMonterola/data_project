<!DOCTYPE html>
<html lang="en">
  <%= content_for(:html_title) {"All Dog Facts"} %>

  <h1 class="title is-1">All Dog Facts</h1>

  <div>
    <ol>
      <% @dog_facts.each do |dog_fact| %>
        <li class="mt-6"><%= link_to dog_fact.dog_fact, dog_fact %></li>
      <% end %>
    </ol>
  </div>

  <div class="mt-6">
    <%= paginate @dog_facts %>
  </div>
</html>
